<h1>ジャンル編集画面</h1>

<%= form_with model: @genre, local:true do |f| %>
<tr>
    <th>ジャンル名</th>
    <th><%= f.text_field :genre %></th>
    <th><%=f.submit "変更を保存", class:"btn btn-sm btn-outline-success"%></th>
</tr>
<% end %>